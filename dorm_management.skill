# 碧苑宿舍點名系統 (Dormitory Management PWA) - 專案維護開發指引

此 Skill 文件定義了維護與擴展「碧苑宿舍點名系統」的核心原則、UI 規範與代碼邏輯。

## 1. 專案概述
本專案是一個為碧苑宿舍開發的高度自訂化 PWA，旨在取代傳統紙本點名。
*   **核心目標**：極致的手感、視覺衝擊（Wow factor）、高效率點名流程。
*   **主要功能**：分中隊點名、總表統計、回報報修、住宿生檔案管理、請假記錄。

## 2. 技術棧與結構
*   **前端**：Vanilla HTML5, CSS3 (CSS Variables, Neumorphism), Vanilla JavaScript (ES6+).
*   **狀態管理**：使用全局 `state` 對象進行數據同步。
*   **佈局架構**：模擬單頁應用 (SPA)，透過控制 `.page` 類別的 `active` 狀態來切換視圖。
*   **外部依賴**：`xlsx.full.min.js` (Excel 處理), `marked.min.js` (Markdown 渲染)。

## 3. UI/UX 設計規範 (核心關鍵)
*   **視覺風格**：
    *   **Dark Mode (預設)**：深色磨砂玻璃 (Glassmorphism) + 輕量級擬物 (Neumorphism)。
    *   **Light Mode**：柔軟的擬物化設計 (Soft UI)，強調陰影與層次。
*   **動畫原則**：
    *   使用 `View Transitions API` 進行主題切換（呈現向外擴散的圓圈效果）。
    *   所有卡片 (`.sq-card`) 在載入時應有 Q 彈的動畫。
    *   切換頁面時使用 `fadeUp` 或 `slide` 動畫。
*   **色彩系統**：使用 CSS Variables (`--purple`, `--green`, `--red` 等) 以確保主題切換一致。

## 4. 代碼邏輯模式
*   **數據更新**：透過 `loadData()` 異步獲取所有 Notion 資料。
*   **實時同步**：系統內置輪詢機制 (`doPoll`)，每 3-10 秒檢查一次 KV 數據中心。
*   **渲染模式**：大部分頁面使用 `render[PageName]` 函數動態生成 HTML 並注入 DOM。
*   **觸覺反饋**：所有重要交互（點擊、切換、成功）都應調用 `haptic()` 函數。

## 5. 常見開發任務與步驟
### A. 新增一個功能頁面
1.  在 `index.html` 中新增 `<div id="page-[name]" class="page">`。
2.  在 `style.css` 中定義該頁面的專屬樣式（若有必要）。
3.  在 `app.js` 中新增 `render[Name]()` 函數。
4.  在 `navigateTo` 邏輯中包含新頁面的初始化腳本。

### B. 修改主題配色
*   修改 `style.css` 中的 `:root` (Dark) 與 `body.light-mode` 變量。
*   **注意**：所有內嵌 SVG 或圖片路徑需通過 `getIconSrc` 處理以適應亮/暗模式。

### C. 擴展 API 功能
*   修改 `api.js` 以增加新的 endpoint 或修改數據解析邏輯。

## 6. 溝通與維護模式
1.  **版本控制與變更記錄**：
    *   每次變更前必須先報告，變更後需詳細記錄於 `updata.txt`。
    *   紀錄格式應包含：版本號、代號（水果/食物）、變更重點（新增/修正/優化）。
2.  **技術說法(通俗易懂的解釋)**：
    *   **重要規則**：在向用戶說明更新計畫、開發方案或技術細節時，嚴禁使用艱澀的術語（如 Race Condition, Polling, Buffer 等）。
    *   **應採用「不懂得人也能懂」的方式**：用擬人化、生動的比喻來解釋程式邏輯確保用戶能輕鬆理解。
3.  **UI 規範**：
    *   堅持擬物化/玻璃擬物化風格，強調觸覺回饋（haptic）。
    *   動畫必須順暢，不允許閃爍。
4.  **謹慎開發與審查**：
    *   **核心提醒**：用戶會使用 Codex 審查你的代碼。
    *   **行為規範**：AI 在編寫代碼時必須更加審慎，確保邏輯無誤、性能優化且完全符合專案既有的美學與架構規範。

## 7. 版本控制與變更記錄 (Versioning & Changelog)
為了確保專案進度透明且版本可控，同時避免更新紀錄過於頻繁（瑣碎小事不需每次更新），必須遵循以下規範：
1.  **事前報告**：在進行任何實際代碼修改前，必須先明確告知用戶準備修改的內容。
2.  **暫存變更（暫不寫入）**：
    *   每次的小修改、代碼優化或單一功能調整應先視為「暫存變更」。
    *   AI 會在對話中追蹤這些更動點，但**不會**立即更新 `updata.txt`。
3.  **打包更新與確認**：
    *   在完成一個大型專案、修復多項關聯 Bug 或完成一系列功能開發後，AI 必須主動向用戶確認：「是否將目前的暫存變更打包紀錄？」
    *   獲得用戶確認後，才將所有累積的更動點彙整，一次性寫入 `updata.txt` 並升級版號。
4.  **紀錄格式**：必須依據以下格式添加至 `updata.txt` 的頂部（最新紀錄在最上方）：

### 版本 [版本號] 代號：[代號] 
- **[類型]** [具體描述偏向APP更新向使用者描述可以生動點]
- 

*   **版本遞增原則**：寫入時查看 `updata.txt` 最新版號，根據改動幅度決定跳號大小。
*   **類型標籤參考**：調整、新增、增加、優化、更新、修正、修復。

## 8. 特殊交互說明
*   **PIN 碼驗證**：進入中隊點名或幹部專區需經過 `showPinDialog`。
*   **報修/意見回饋**：支持圖片上傳，並使用自訂的動畫按鈕 (`.feedback-send-btn`)。

---
**維護提醒**：本專案非常重視「高級感」，任何新加入的 UI 元素都必須經過 CSS 變量與陰影處理，嚴禁使用瀏覽器默認樣式或粗糙的色彩。
